{
  "changelogCommand": "CHANGELOG.MD < it log --pretty=format:'* %s (%h)' [REV_RANGE]",
  "buildCommand": "make lint && make test && make build",
  "github": {
   "release": true,
   "tokenRef": "GITHUB_TOKEN"
  }
}
